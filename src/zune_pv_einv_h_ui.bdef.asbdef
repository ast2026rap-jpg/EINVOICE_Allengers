projection;
strict ( 2 );
use draft;

define behavior for ZUNE_PV_EINV_H_UI //alias <alias_name>
{
  use create;
  use update;
  use delete;
  field(readonly)Sapid,Createdat,Createdby,Lastchangeat,Lastchangedby;
  use action GenerateEinvoice;
  use action GenerateEWaybill;
  use action Activate;
  use action Discard;
  use action Edit;
  use action Resume;
  use action Prepare;
  use action CancelEInv;
  use action CancelEWaybill;
}